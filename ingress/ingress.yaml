apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: app-ingress
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: result.dudeyouhavenoidea.me
      http:
        paths:
          - backend:
              service:
                name: result-service
                port:
                  number: 80
            pathType: Prefix
            path: "/"

    - host: voting.dudeyouhavenoidea.me
      http:
        paths:
          - backend:
              service:
                name: voting-service
                port:
                  number: 80
            pathType: Prefix
            path: "/"

    - host: ockshop.dudeyouhavenoidea.me
      http:
        paths:
          - backend:
              service:
                name: front-end
                port:
                  number: 80
            pathType: Prefix
            path: "/"
